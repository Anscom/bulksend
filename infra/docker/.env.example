# Copy this file to .env in the same directory before running docker compose up.

# PostgreSQL
POSTGRES_USER=bulksend
POSTGRES_PASSWORD=changeme
POSTGRES_DB=bulksend
POSTGRES_PORT=5432

# Redis
REDIS_PASSWORD=changeme
REDIS_PORT=6379

# Kafka
KAFKA_PORT=9092
KAFKA_CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qk   # stable for dev; generate with kafka-storage random-uuid
KAFKA_CONSOLE_PORT=8080
